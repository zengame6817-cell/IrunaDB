# Development

## 開発方針

- データと画面を分離する
- スプレッドシートをデータの正本とする
- JavaScriptファイルは役割ごとに分割する
- 1リリースごとに動作する状態を維持する

## JavaScriptの役割

- `config.js`：環境設定
- `utils.js`：共通処理
- `api.js`：GAS API通信
- `modal.js`：詳細画面
- `ui.js`：一覧・検索・分類表示
- `app.js`：状態管理と起動処理

## バージョン方針

- `0.1.x`：データベース一覧
- `0.2.x`：効果・条件表示
- `0.3.x`：検索・図鑑強化
- `0.4.x`：ビルドシミュレーター
- `1.0.0`：正式版
